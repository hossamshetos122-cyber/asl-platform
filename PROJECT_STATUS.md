# PROJECT_STATUS — تقرير فحص المشروع الشامل

**تاريخ الفحص:** 29 أغسطس 2026 — **آخر commit منشور:** `d45c8a8` (نُشر على Vercel Production بنجاح ✅)

---

## 1. قاعدة البيانات: حقيقية وليست وهمية ✅

المشروع متصل بقاعدة بيانات **PostgreSQL حقيقية على Neon (سحابة)** عبر Prisma ORM:

- ملف `.env` يحتوي على اتصال حقيقي: `DATABASE_URL="postgresql://neondb_owner:...@ep-autumn-recipe-...neon.tech/neondb?..."`
- **البيانات الظاهرة في الموقع (الفرق، المباريات، الترتيب، الهدافين) ليست مكتوبة في الكود** — كلها تُقرأ مباشرة من قاعدة البيانات عبر `prisma` (مثال: `src/lib/data/home.ts`, `src/lib/data/teams.ts`).
- يوجد ملف seeding (`prisma/seed.ts`) زُرع مرة واحدة أدخل 20 فريق + 360 لاعب + بطولات + مباريات + أهداف. الرقم الظاهر على الموقع 21 فريق يعني أنك أنشأت يدوياً أو أُضيف فريق إضافي بعد البذر.
- الموقع الحي حالياً يعرض: **21 فريق، 116 هدف، 3 بطولات، 361 لاعب** — كلها مشروعة من قاعدة البيانات.
- **الأدلة:** `schema.prisma` يحتوي 17 نموذج (User, Team, Player, Match, Tournament, MatchEvent...) مع علاقات كاملة.

## 2. المصادقة: نظام حقيقي شغال ✅

نظام تسجيل دخول **مخصص (Custom Auth)**، ليس نموذجاً شكلياً فقط:

- كلمة المرور تُشفّر بـ **scrypt** (مِلح عشوائي 16 بايت) — لا تُحفظ كنص.
- الجلسات محفوظة في جدول `Session` مع فترة انتهاء 7 أيام، وكوكي httpOnly.
- `/register` ينشئ مستخدماً حقيقياً في قاعدة البيانات ويحوّل للمستخدم بعدها.
- `/login` يتحقق من البريد/كلمة المرور مقابل قاعدة البيانات، يفرض **rate limiting (5 محاولات/15 دقيقة)**، **يلغي كل الجلسات القديمة** عند تسجيل الدخول، ويتحول بالمدير لـ `/admin` وبالمشجع لـ `/dashboard`.
- `/logout` يلغي الجلسة من قاعدة البيانات.
- middleware يحمي `/admin` و `/dashboard` (يُعيد غير المسجل للصفحة login).
- **حسابات البذر:** بيانات الدخول الدموية (admin/organizer) في `prisma/seed.ts` لها كلمة مرور مولّدة عشوائياً **تُطبع فقط عند تشغيل السيد**. كلمة مرور الأدمن الحقيقية **لا تُحفظ في أي ملف في المستودع** — تُطبَّق على قاعدة البيانات الحية عبر `npm run db:rotate:admin` (يقرأ القيم من `.env` المُهمل).

## 3. صفحة /teams/new: ليست فاضية — تحتاج تسجيل دخول ✅

الصفحة مكتوبة بالكامل (`src/app/teams/new/page.tsx`) وتعمل هكذا:

1. أول ما تفتح تعرض **شكل skeleton** (تأكيد تحميل) أثناء فحص الجلسة.
2. بعد الفحص، إذا **غير مسجل**: تعرض رسالة واضحة "يجب تسجيل الدخول أولاً لإنشاء فريق" + زر دخول (هذا على الأغلب سبب ظهورها "فاضية" بالنسبة لك لأنها لا تعرض نموذجاً قبل الدخول).
3. إذا **مسجل**: تعرض النموذج كاملاً (شعار الفريق + الاسم + المختصر + المدينة) ويعمل فعلاً.

**لا يوجد خطأ في الكونسول** — لا deficient. فقط قم بتسجيل الدخول أولاً (أو أنشئ فريقاً من زر "إنشاء فريق" داخل `/dashboard` وهو متاح للمسجل).

## 4. صفحات /register و /login: تحفظ بيانات فعلياً + validation كامل ✅

- **/register:** ينشئ حساباً حقيقياً (User) في قاعدة البيانات، يشفر كلمة المرور، ينشئ جلسة فوراً، redirect لـ `/dashboard`. يوجد **validation بالكامل** عبر Zod: الاسم مطلوب، البريد صيغة صحيحة، كلمة المرور 8–128 حرف، تطابق تأكيد كلمة المرور، ورسائل خطأ عربية تحت كل حقل، + **rate limit (3 تسجيلات/30 دقيقة)**.
- **/login:** تحقق كامل ضد قاعدة البيانات + validation (البريد/كلمة المرور) + rate limit + رسائل عربية، + دعم `?redirect=` مع حماية من redirect مفتوح.

## 5. إدارة النتائج والترتيب: لوحة تحكم — وليس تعديل كود ✅

- **تحديث النتائج:** من **لوحة التحكم للمدير** (`/admin/matches`) يوجد نموذج ScoreUpdateForm تحت كل مباراة انتهت/مباشرة — تُدخل نتيجة (مثلاً 2-1) وتضغط "تحديث" فتُحفظ في قاعدة البيانات فوراً.
- **الترتيب والهدافين:** لا يُخزنان — **يُحسبان تلقائياً** عند كل قراءة من المباريات المنتهية (`getStandings`) والأهداف (`getTopScorers`) في `src/lib/data/home.ts`، بما فيها نقاط/فوز/تعادل/خسارة وترتيب المواجهات المباشرة. أي تحديث للنتيجة يحدّث الصفحات تلقائياً (revalidation).
- تغيير حالة المباراة (مجدولة→انتهت...) يتم من نموذج الإنشاء/التعديل في لوحة التحكم (حقل status).

## 6. متغيرات البيئة (Environment Variables)

| المتغير | مطلوب؟ | الحالة |
|---|---|---|
| `DATABASE_URL` | **مطلوب (وحيد الضروري)** | ✅ موجود في `.env` محلياً وموجود في Vercel (الموقع يعمل) |
| `SESSION_COOKIE_NAME` | اختياري | الاستمرار الافتراضي `session` — غير مضبوط (طبيعي) |
| `NODE_ENV` | تلقائي | يضعه Vercel تلقائياً |

**⚠️ تنبيه أمني يجب أن تتعامل معه بنفسك:**
- يوجد ملف `.env.txt` مرفوع في git (يحوي بيانات Neon كاملة). أضافه الماضي لملف `.gitignore` **ولكنه لا يزال موجوداً في تاريخ git** — يجب **إعادة تعيين كلمة المرور (rotate) لقاعدة بيانات Neon** من لوحة Neon، ويمكن حذف الملف من التاريخ بأداة `BFG Repo-Cleaner` أو عمل repo جديد. لا أستطيع القيام بهذين الإجراءين داخلياً لأنهما يتطلبان دخولاً لمواقع خارجية بحسابك.
- لا توجد أي بيانات سرية أخرى في الكود.

## 7. حالة الكود العامة: نظيف — مع ملاحظات صغيرة ⚠️

✅ لا توجد أي تعليقات `TODO` أو `FIXME` متروكة.
✅ الاختبارات 59/59 ناجحة، TypeScript 0 أخطاء، ESLint 0 تحذيرات، Build ناجح (24 مساراً).
- **كود غير مستخدم (بسيط):** دالتا `addMatchEvent` و `addToTeam` موجودة ومختبرة لكن **لا يوجد واجهة حالياً تستدعيهما** (إضافة الأهداف/الأحداث تتم حالياً عبر البذر أو `prisma studio`). غير ضار — قد تُستخدم لاحقاً لعرض أحداث المباراة.
- مسار `/admin/seed` (POST فقط) محمي بصلاحية المدير، لإعادة ملء بيانات تجريبية — لا خطر.
- **ملاحظة تقنية صغيرة:** تحقق `cuid` للـ IDs كان صارماً جداً ويمنع تعديل/حذف الفرق واللاعبين المضروبين من البذر (التي تحمل IDs مثل `team-karnak`) — **تم إصلاحه في آخر commit `d45c8a8` ونشر بنجاح**. الآن المدير يستطيع تعديل وحذف كل الفرق واللاعبين (المضروبة والمُضافة) بأمان.

---

## ✅ الخلاصة

- **قاعدة البيانات:** حقيقية (Neon PostgreSQL) ✅
- **المصادقة:** حقيقية وشغالة بالكامل ✅
- **/teams/new:** مكتوبة وتعمل لكنها تتطلب تسجيل دخول ✅
- **/login و/register:** يحفظوان فعلياً مع validation ورسائل عربية ✅
- **النتائج والترتيب:** لوحة تحكم للمدير + حساب تلقائي ✅
- **البيئة:** المتغير الضروري `DATABASE_URL` موجود محلياً وفي Vercel ✅

**الإجراء الوحيد المطلوب منك شخصياً (وليس من الكود):**
1. تغيير كلمة مرور Neon من لوحة Neon (لأن `.env.txt` كان في git).
2. (اختياري) إنشاء حساب ممثّل/تلطيخ اسم تجاري؛ الموقع جاهز للاستخدام الفعلي.